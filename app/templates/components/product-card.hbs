{{#link-to linkName link_id}}
  <div class="card z-depth-2">
    <div class="card-image hoverable">
      <img class="responsive-img" src="{{pictureUrl}}">
      <span class="card-title">{{description}}</span>
    </div>
  </div>
{{/link-to}}
