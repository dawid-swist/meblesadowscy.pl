{{foto-slider silderElements=model.sliders}}
{{ ms-products }}
{{!--
{{#each model.articles as |item| }}

  {{ section-article
    titleID=item.ShortUrl
    body=item.Body
    title=item.title
    imageUrl=item.ImageUrl
    imageAlt=item.ImageAlt
    backgroundColor=item.BackgroundColorCss
   }}

{{/each }} --}}

{{outlet}}
